<%= form_for [@user, @fleet, @battlegroup] do |f| %>
 
 	<%= f.collection_select(:battlegroup_type_id, BattlegroupType.all, :id, :name) %>

	<%= f.submit "Create Battlegroup" %>
<% end %>
<!-- trade out for individual links -->