<li>
	<%= link_to show_name(fleet), user_fleet_path(@user.id, fleet) %>
	
	<p><%= show_faction(fleet) %></p>
	
	<p><%= show_points(fleet) %></p>
	
	<!-- add data: { confirm: "confirmation text" } if you want -->
	<%= link_to 'X', user_fleet_path(@user, fleet), method: :delete %>
</li>